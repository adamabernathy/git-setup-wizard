# name: Tests

# on:
#   push:
#     branches: [main]
#   pull_request:
#     branches: [main]

# jobs:
#   test:
#     runs-on: ${{ matrix.os }}
#     strategy:
#       matrix:
#         os: [macos-latest]
#         python-version: ["3.9", "3.11", "3.12"]

#     steps:
#       - uses: actions/checkout@v4

#       - name: Set up Python ${{ matrix.python-version }}
#         uses: actions/setup-python@v5
#         with:
#           python-version: ${{ matrix.python-version }}

#       - name: Install dependencies
#         run: |
#           python -m pip install --upgrade pip
#           pip install rich pytest

#       - name: Run tests
#         run: pytest tests/ -v

#   lint:
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v4

#       - name: Set up Python
#         uses: actions/setup-python@v5
#         with:
#           python-version: "3.12"

#       - name: Install dependencies
#         run: |
#           python -m pip install --upgrade pip
#           pip install rich ruff

#       - name: Check syntax
#         run: python -m py_compile git_setup_wizard.py

#       - name: Ruff lint
#         run: ruff check git_setup_wizard.py tests/

#       - name: Ruff format check
#         run: ruff format --check git_setup_wizard.py tests/
